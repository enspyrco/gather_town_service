{
  "name": "gather-town-service",
  "version": "0.0.1",
  "description": "An RPC for the gather.town websocket API.",
  "main": "index.js",
  "engines": {
    "node": ">= 12.0.0"
  },
  "scripts": {
    "start": "node index.js",
    "test": "mocha test/app.test.js --check-leaks",
    "system-test": "mocha test/system.test.js --timeout=360000 --exit"
  },
  "dependencies": {
    "express": "^4.16.4"
  },
  "devDependencies": {
    "google-auth-library": "^7.0.0",
    "got": "^11.5.0",
    "mocha": "^9.0.0",
    "sinon": "^14.0.0",
    "supertest": "^6.0.0",
    "uuid": "^8.0.0"
  }
}
